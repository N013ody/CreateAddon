# CreateAddon 设计准则与方块方案

本文档用于指导后续 CreateAddon 方块与机械组件设计。所有设计必须尽量贴近《机械动力》（Create）主模组的设计哲学：可见的物理传动、优雅的机械结构、低 UI 依赖、世界级交互、可通过 Ponder 直观教学。

## 核心设计准则

### 三位一体

每个新增方块都必须同时回答三个问题：

- 功能性：它解决什么机械问题？是否提高自动化、导航、制导、检测或加工效率？
- 装饰性：它是否可以自然融入 Create 的黄铜、铜、安山合金、铁框架建筑语言？
- 可玩性：玩家是否能通过观察运动部件、声音、粒子或实体交互理解它在工作？

不允许只做“一个黑盒机器”。方块必须让玩家看见机械逻辑。

### 物理传动

禁止使用电缆、电池、EU/RF 能量。动力来源只能来自：

- 旋转力：轴、齿轮、皮带、离合、变速箱。
- 流体力：水流、风压、泵压。
- 物理接触：碰撞、对接、机械臂、移动结构接触。
- 红石只能作为控制信号，不能作为“能量”。

每个主动方块都应明确：

- 输入轴在哪一面？
- 是否消耗 Stress？
- 转速是否影响范围、精度、频率或强度？
- 是否能在移动结构上工作？

### 去 UI 化与可视化

避免 GUI 进度条和抽象数字面板。信息应优先通过世界内表现传达：

- 指针、刻度盘、灯泡、机械翻牌。
- 旋转天线、摆动陀螺、伸缩探针。
- 激光、扫描线、光点、实体投影。
- 物品在世界中移动，而不是存在隐藏容器里。

调试 HUD 可以存在，但不能成为主要玩法。

### 动态结构

优先考虑 Contraptions：

- 方块应能安装在飞行器、车辆、船体、炮塔、旋转平台上。
- 传感器应优先使用自身位置、速度、朝向推导数据。
- 控制器应输出可被机械结构使用的红石、旋转或物理状态。

### 直觉式教学

每个机制必须能写成一个简洁 Ponder 场景：

- 展示动力接入。
- 展示运动部件变化。
- 展示输入对象。
- 展示输出结果。
- 展示一个实际用途。

如果一个机制无法用 30 秒 Ponder 讲清楚，说明设计过于抽象。

## 推荐设计方向

当前模组主题是 Guidance & Control，即机械制导、传感器融合、自动控制。后续方块应围绕“看见目标、测量状态、机械修正、物理执行”展开。

## 方块方案一：Laser Designator / 激光指示器

### 分类

传感器 / 指向性设备 / 制导组件。

### 设计目的

为激光制导系统提供世界级目标标记。它不是简单 GUI 选目标，而是用真实可见的激光束照亮目标点。

### 物理运作逻辑

- 后部接入旋转轴。
- 转速决定有效距离与光束稳定度。
- 低速时光束短且闪烁，高速时光束长且稳定。
- 输入轴可接离合器，用于机械开关。
- 如果安装在转台、旋转框架或 Contraption 上，光束方向随结构移动。

### 世界交互

- 右击实体：锁定该实体为照射目标。
- 潜行右击：清除锁定，改为沿方块朝向直线照射。
- 照射到方块时，在命中点显示红色小光斑。
- 照射到实体时，在实体表面显示可见光斑，但不直接造成伤害。
- 输出数据：命中类型、命中坐标、命中距离。

### 美学建议

- 主体：铜机壳。
- 发射环：黄铜。
- 透镜：红石灯或红色玻璃风格。
- 后部：安山合金轴承。
- 推荐搭配：旋转框架、机械轴承、显示链接、红石链接。

### Ponder 场景

1. 接入水车或创造马达。
2. 激光射出并命中墙面。
3. 放入移动实体，光点停在实体表面。
4. 用阈值控制器读取距离，触发红石灯。

## 方块方案二：Beam Rider Receiver / 驾束接收器

### 分类

制导传感器 / 移动结构组件。

### 设计目的

让移动结构知道自己是否位于激光束中心。用于激光驾束制导、自动对准、无人飞行器路线保持。

### 物理运作逻辑

- 被动传感器，不需要动力，但需要安装在 Contraption 上才有实际用途。
- 若附近存在激光束穿过，则检测自身与光束中心线的偏离量。
- 偏离量通过机械指针和灯泡显示。
- 输出 double：横向偏差，0 表示正中。

### 世界交互

- 靠近激光束时，接收器上的小红石透镜点亮。
- 离束越远，侧向指针偏转越大。
- 可被 Guidance Computer 读取，用于修正 rudder/elevator。

### 美学建议

- 小型黄铜框架 + 红色接收片。
- 适合装在飞行器头部、导弹尾部、炮塔校准架上。

### Ponder 场景

1. 一条激光束穿过场景。
2. 移动接收器偏离光束，指针偏转。
3. 接收器回到中心，灯变亮。
4. Guidance Computer 输出修正命令。

## 方块方案三：Threshold Controller / 阈值控制器

### 分类

控制器 / 红石桥接器。

### 设计目的

将传感器数值转成世界内红石信号。它不是计算机 GUI，而是机械比较器。

### 物理运作逻辑

- 不消耗旋转力。
- 扫描附近传感器。
- 通过机械翻牌或指针显示当前比较符号。
- 满足条件时点亮红石灯并输出 15 强度红石。

### 世界交互

- 右击切换监听传感器。
- 潜行右击切换比较符号。
- 通过聊天或后续实体化旋钮设置阈值。
- 推荐未来改造成“黄铜刻度旋钮”，避免纯文字输入。

### 美学建议

- 黄铜外壳、红石灯、机械翻牌式比较符号。
- 可与红石链接、阈值灯、机械离合器搭配。

### Ponder 场景

1. 激光测距仪测量墙面距离。
2. 阈值控制器设为 `< 8`。
3. 移动墙面靠近，红石灯亮。
4. 红石输出关闭离合器或触发发射机构。

## 方块方案四：Rotary Frame / 旋转框架

### 分类

结构件 / 装饰件 / 指向性设备底座。

### 设计目的

允许玩家把任意方块外观装入框架，并以 360 度角度旋转。用于自定义炮塔、传感器、灯、激光器、摄像头、天线等指向性设备。

### 物理运作逻辑

- 框架本身不产生动力。
- 未来可接入旋转轴，让 yaw 或 pitch 随 RPM 缓慢转动。
- 当前支持手动角度调整，作为美术和定位基础。

### 世界交互

- 手持方块右击：填充外观。
- 空手右击：调整 yaw。
- 空手潜行右击：调整 pitch。
- 未来扩展：扳手细调 1 度，普通右击 15 度。

### 美学建议

- 铜/黄铜空心框。
- 角落有铆钉或轴承。
- 适合搭配机械轴承、转台、炮塔底座。

### Ponder 场景

1. 放置旋转框架。
2. 手持方块填充。
3. 右击旋转 yaw，潜行右击旋转 pitch。
4. 在框架上方安装激光指示器，展示任意角度指向。

## 方块方案五：Optical Tripwire / 光束绊线器

### 分类

红石检测器 / 安防组件。

### 设计目的

使用激光束作为实体检测线，替代普通绊线。适合门禁、自动炮塔、对接检测。

### 物理运作逻辑

- 需要激光穿过检测器窗口。
- 光束被实体或方块阻挡时输出红石。
- 不需要能量，仅是被动检测器。

### 世界交互

- 红色小窗口被激光照亮。
- 阻挡时窗口变暗，侧面红石灯亮。

### 美学建议

- 小型黄铜门框 + 红石玻璃片。
- 可用于基地门、飞行器甲板、安全栅栏。

### Ponder 场景

1. 激光照射光束绊线器。
2. 玩家穿过光束。
3. 红石灯亮，机械门关闭。

## 方块方案六：Mechanical Gimbal / 机械万向节

### 分类

传动件 / 指向机构。

### 设计目的

让激光器、雷达、炮塔等指向性设备获得可见的双轴机械瞄准能力。

### 物理运作逻辑

- 一个输入轴控制 yaw。
- 另一个输入轴控制 pitch。
- 转速决定转动速度。
- 应力消耗随负载方块质量或尺寸增加。

### 世界交互

- 装上激光器后可机械扫射。
- 与阈值控制器联动可做自动搜索或扇区扫描。

### 美学建议

- 双层黄铜环、中心铜轴承、外侧齿轮齿圈。
- 应明显展示两个旋转自由度。

### Ponder 场景

1. 安装万向节。
2. 分别接入两根轴。
3. yaw/pitch 分别转动。
4. 激光器随万向节扫过目标。

## 后续设计优先级

1. Laser Designator：让激光从测距工具变成制导工具。
2. Beam Rider Receiver：实现真正的激光驾束制导。
3. Mechanical Gimbal：给所有指向设备提供 Create 风格的物理瞄准机构。
4. Optical Tripwire：把激光转成更直观的世界级红石玩法。
5. Rotary Frame 扩展：接入动力轴，实现机械驱动角度。

## 不推荐的设计

- 电池供能激光炮。
- 打开 GUI 输入目标坐标的导弹电脑。
- 纯数字面板，没有实体运动反馈。
- 无动画的方块贴图机器。
- 只能放地上、不能装在移动结构上的传感器。

CreateAddon 的正确方向应是：让机械结构“看见世界”，再用齿轮、轴、指针和红石把这些观察转化成动作。
